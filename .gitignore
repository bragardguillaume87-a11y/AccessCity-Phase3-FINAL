node_modules/
.DS_Store
# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Playwright outputs
test-results/
playwright-report/
*.webm
**/trace.zip
# PNG exclus uniquement pour les outputs Playwright (pas les assets projet)
playwright-report/**/*.png
test-results/**/*.png

# Coverage
coverage/
.nyc_output/

# Build / cache
.cache/
dist/
dist-ssr/
dist-player-tauri/
tmp/

# Tauri build artifacts (commiter uniquement public/player-win.exe)
src-tauri/target/

# OS artifacts
Thumbs.db
ehthumbs.db
Desktop.ini

# IDE
.vscode/
*.code-workspace

# Misc
*.local

# Environment variables (NEVER commit real keys)
.env
.env.*.local
.env.local

# Secrets and API keys
.apikey
*.secret
.claude
.anthropic

# Temporary files with problematic names
pace*
resultats*

# Perplexity context files (compilation documents for external AI)
.perplexity-context/

# Assets uploadés — tous trackés dans git pour sync entre machines
# (supprimer cette section si les fichiers deviennent trop volumineux → passer à Git LFS)